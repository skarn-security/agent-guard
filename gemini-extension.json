{
  "name": "skarn",
  "description": "Security scanner for Gemini CLI: finds leaked credentials and secrets in your sessions, vets the hooks and MCP servers the CLI loads, and runs a pre-execution guard that reports the tool calls it would have flagged. Everything runs locally on the Skarn engine and every finding comes back redacted.",
  "mcpServers": {
    "skarn": {
      "command": "skarn",
      "args": [
        "mcp"
      ]
    }
  },
  "contextFileName": "GEMINI.md",
  "version": "0.29.0"
}
